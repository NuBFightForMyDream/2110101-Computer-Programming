def soundex(code) : # input code -> return soundex code
    
    # step 1 : cut h and w first
    code_cut = ''
    for char in code :
        if char not in 'HW' : code_cut += char
        
    # step 2 : 
        
    
    